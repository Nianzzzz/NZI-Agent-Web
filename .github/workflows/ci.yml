name: CI

on:
  pull_request:
    types: [opened, synchronize, reopened]
  push:
    branches: [main, feature/*]

# 同一 PR 的多次推送自动取消旧构建，节省 CI 时长
concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref || github.ref }}
  cancel-in-progress: true

jobs:
  typecheck-and-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 9
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - name: Install dependencies
        run: pnpm install

      - name: Build shared-types (生成 dist，供后端 path mapping 解析)
        run: pnpm --filter @nzi/shared-types build

      - name: Prisma generate (生成 PrismaClient 类型)
        run: pnpm --filter @nzi/backend generate

      - name: Typecheck & Build backend (tsc 同时承担类型检查 + 编译)
        run: pnpm --filter @nzi/backend build

      - name: Typecheck & Build frontend (next build 包含类型检查)
        run: pnpm --filter @nzi/frontend build

  backend-test:
    runs-on: ubuntu-latest
    # 无真实 DB/Redis 时跳过（本地 mock 测试）
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 9
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install
      - run: pnpm --filter @nzi/backend generate
      - name: Run backend tests (if any)
        run: pnpm --filter @nzi/backend test || true
